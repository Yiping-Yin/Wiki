{"id":"unsw-infs-3822__hands-on-logistic-regression-week-3","title":"Hands On logistic regression Week 3","body":"                                                    INFS3822– AI for Business Analytics\n\n\n\nWeek 3 Hands-On Exercise\nStructured data classification using logistic regression in Python / R\n\nPre-requisites\nPlease complete all Week 3 Preparation Activities, available on Moodle, under Week 3 section.\n\nObjective\nTrain a model in Python or R to classify structured data into a Boolean outcome\n\nData\nAny CSV file that has one field/variable as a Boolean outcome variable to predict. We will use a synthetic\ndataset that has 24 columns (variables), one of which is a Boolean to be used as the outcome variable\n(to predict). The dataset is provided on Moodle and named “data_exercise.csv”.\n\nModel type\nLogistic Regression\n\nSetup\n•   Download the dataset from Moodle as a CSV file called data_exercise.csv\n•   Visit Google Colab at https://colab.research.google.com/ (make sure you are signed-in to a Google\n    account). You can learn more about what Google Colab is by watching the video below:\n\n\n\n\n                          https://youtu.be/inN8seMm7UI?si=13bM9anVHQrXc_7m\n•   In Google Colab, you can create Python or R notebooks to run analytics. Notebooks can be\n    created from scratch, or uploaded as .ipynb files. Two notebook files are available on Moodle:\n        o Week3_Hands_On_Exercise_Python.ipynb\n\f        o Week3_Hands_On_Exercise_R.ipynb\n    They contain Python and R code respectively for this Hands-On exercise. These files are ready for\n    you to download from Moodle (under Week 3 section), then upload to Google Colab. Feel free to\n    select your language of choice (Python or R) for this activity.\n\n    Alternatively, if you would like to create a notebook from scratch and populate it with Python or R\n    code, follow the links below:\n        o Create a notebook by visiting https://colab.research.google.com/#create=true or simply click\n           File → New notebook (Python is the default language on Google Colab).\n        o If you would like to use R as the runtime language on your new notbook, click “Runtime”,\n           then “Change runtime type” and select\n\n\n\n\n•   Once your notebook is uploaded/created and opened, you need to upload the dataset\n    data_exercise.csv. Simply open the Files tab in your notebook and upload the file.\n\n\n\n\n    •   Run your notebook step by step until all cells are executed successfully.\n\nRefer to the Python and R notebooks below for the code to be used.\n\f                                            PYTHON CODE\n\n\n\n\nLoad the dependencies\nFirst, we need to load the necessary libraries. This includes:\n\n   •   The pandas library, which is used for data manipulation and analysis.\n   •   The train_test_split function to split the data into training and testing sets.\n   •   The LogisticRegression class, which is a linear model for binary classification\n   •   The accuracy_score function which is used to measure the performance of a classifier model.\n\n import pandas as pd\n from sklearn.model_selection import train_test_split\n from sklearn.linear_model import LogisticRegression\n from sklearn.metrics import accuracy_score\n from sklearn import preprocessing\n\n\nLoad the data\nFirst, download the dataset from Moodle as a CSV file called data_exercise.csv. Once downloaded,\nupload the csv file on your Colab notebook.\nLet's load the dataset from the csv file provided as follows:\n # Load the data\n data = pd.read_csv(\"data_exercise.csv\")\n   Explanation of the code: data = pd.read_csv(\"data_exercise.csv\") reads the data from 'data.csv' file and\n   loads into data variable using the read_csv function from the pandas library (aliased as pd).\n\n\n\nSplit the data into inputs (X) and outputs (y)\nSplit the data into inputs and outputs. The inputs consist of all available variables (columns) in the data\nexcept the last column called \"outcome\". The output is the variable \"outcome\" itself.\nNow, let's split the data into input (X) and output (y) variables as follows:\n # Split the data into inputs (X) and outputs (y)\n X = data.drop(\"outcome\", axis=1)\n y = data[\"outcome\"]\n   Explanation of the code: data.drop(\"outcome\", axis=1) and y = data[\"outcome\"] takes the input and output\n   columns from the loaded dataframe. Here the \"outcome\" column is taken as output and rest are taken as input.\n\n\n\nPreprocess the data (scaling)\nScale the input data to ensure that all the input features are on the same scale. It standardizes the data\nby subtracting the mean and dividing by the standard deviation.\n\f# Preprocess the data (scaling)\nscaler = preprocessing.StandardScaler().fit(X)\nX = scaler.transform(X)\n\n\nSplit the data into training and testing sets\nSplit the data into training and testing sets. The training set will be used to train the model, while the\ntesting set will be used to test the accuracy of the model.\n# Split the data into training and testing sets\nX_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)\n   Explanation of the code: Split the input and output data using the train_test_split function into training and\n   testing set with test_size =0.2 meaning that the testing set corresponds to 20% of the data.\n\n\n\nTrain a logistic regression model on the training data\nLet's Create a logistic regression model and train it using the training dataset\n# Train a logistic regression model on the training data\nmodel = LogisticRegression()\nmodel.fit(X_train, y_train)\n   Explanation of the code: model = LogisticRegression() creates an instance of the LogisticRegression model.\n   model.fit(X_train, y_train) trains the logistic regression model using the training data.\n\n\n\nMake predictions on the testing data\n# Make predictions on the testing data\ny_pred = model.predict(X_test)\n   Explanation of the code: model.predict function is used to predict the outcome of the testing data (X_test).\n\n\n\nCalculate the accuracy of the model\nCalculate the accuracy of the model by comparing the predictions with the true values in y_test\n# Calculate the accuracy of the model\naccuracy = accuracy_score(y_test, y_pred)\nprint(\"Accuracy:\", accuracy)\n       Explanation of the code: accuracy = accuracy_score(y_test, y_pred) calculates the accuracy of the\n       model. print allows to display the calculated accuracy.\n\f                                                    R CODE\n\n\n\n\nLoad the data\nFirst, download the dataset from Moodle as a CSV file called data_exercise.csv. Once downloaded,\nupload the csv file on your Colab notebook.\nLet's load the dataset from the csv file provided as follows:\n # Load the data\n data <- read.csv(\"data_exercise.csv\")\n   Explanation of the code: data <- read.csv(\"data_exercise.csv\") reads the data from 'data_exercise1.csv' file\n   and loads into data variable using the read.csv function.\n\n\n\nSplit the data into inputs (X) and outputs (y)\nSplit the data into inputs and outputs. The inputs consist of all available variables (columns) in the data\nexcept the last column called \"outcome\". The output is the variable \"outcome\" itself.\nNow, let's split the data into input (X) and output (y) variables as follows:\n # Split the data into inputs (X) and outputs (y)\n X <- data[,-ncol(data)]\n y <- data[,ncol(data)]\n   Explanation of the code: X <- data[,-ncol(data)] and y <- data[,ncol(data)] takes the input and output\n   columns from the loaded dataframe. Here the last column is taken as output and rest are taken as\n   input.\n\n\nPreprocess the data (scaling)\nScale the input data to ensure that all the input features are on the same scale. It standardizes the data\nby subtracting the mean and dividing by the standard deviation.\n # Scale the input data\n X <- as.data.frame(scale(X))\n\n\nSplit the data into training and testing sets\nSplit the data into training and testing sets. The training set will be used to train the model, while the\ntesting set will be used to test the accuracy of the model.\n # Split the data into training and testing sets\n set.seed(123) # For reproducibility\n n <- nrow(X)\n splitIndex <- sample(1:n, size = round(0.8 * n), replace = FALSE)\n X_train <- X[splitIndex,]\n y_train <- y[splitIndex]\n X_test <- X[-splitIndex,]\n y_test <- y[-splitIndex]\n\f   Explanation of the code: splitIndex <- sample(1:n, size = round(0.8 * n), replace = FALSE) allows to split the\n   input and output data into training and testing set with size = 0.8 * n (the training dataset is 80% of the size of\n   the dataset, no being the number of rows).\n   X_train <- X[splitIndex,], y_train <- y[splitIndex], X_test <- X[-splitIndex,] and y_test <- y[-splitIndex] split the\n   input and output data into training and testing set\n\n\n\nTrain a logistic regression model on the training data\nLet's Create a logistic regression model and train it using the training dataset\n# Train a logistic regression model on the training data\nmodel <- glm(y_train ~., data = X_train, family = binomial())\n   Explanation of the code: model <- glm(y_train ~., data = X_train, family = binomial()) trains the logistic\n   regression model using the glm function, with the formula y_train ~., data=X_train, family = binomial(). The\n   formula is used to specify the outcome column, independent variables and the type of model.\n\n\n\nMake predictions on the testing data\n# Make predictions on the testing data\ny_pred <- predict(model, newdata = X_test, type = \"response\")\ny_pred <- ifelse(y_pred > 0.5, 1, 0)\n   Explanation of the code: y_pred <- predict(model, newdata = X_test, type = \"response\") makes predictions\n   on the testing data using predict function, with newdata = X_test and type = \"response\" to get the probability\n   of outcome.\n   y_pred <- ifelse(y_pred > 0.5, 1, 0) threshold the probability values to get binary values, if probability is greater\n   than 0.5 then it's 1 otherwise 0\n\n\n\nCalculate the accuracy of the model\nCalculate the accuracy of the model by comparing the predictions with the true values in y_test\n# Calculate the accuracy of the model\naccuracy <- mean(y_pred == y_test)\nmessage(\"Accuracy: \", accuracy)\n       Explanation of the code: accuracy <- mean(y_pred == y_test) calculates the accuracy of the model by\n       comparing the predictions with the true values in y_test.\n\n       message(\"Accuracy: \", accuracy) prints the accuracy of the model.\n\f"}