{"id":"unsw-infs-3822__hands-on-deep-dive-fraud-detector-using-deep-learning-week-3","title":"Hands On Deep Dive Fraud Detector using Deep Learning Week 3","body":"INFS3822– AI for Business Analytics\n\nWeek 3 Hands-On Deep Dive Pre-requisites Please complete all Week 3 Preparation Activities, available on Moodle, under Week 3 section.\n\nLet’s build a Credit Card Fraud Detector using Deep Learning in Python or R We would like to predict if a credit card transaction is genuine or fraudulent. To do that, we will train an AI model using a dataset of anonymized credit card transactions made by European cardholders for 2 days in September 2013 (Source: https://www.kaggle.com/mlg-ulb/creditcardfraud). Transactions are labelled as fraudulent or genuine, where we have 492 frauds out of 284,807 transactions (0.172% of all transactions). For this Hands-On activity, we will use a smaller smaple of 20,000 transactions provided on Moodle as a csv file called creditcard_sample.csv. Due to confidentiality issues, the original features and background information about the data is not provided. Instead, features called V1, V2, …, V28 are provided as numeric values which are the result of a PCA transformation. In addition, ‘Time’ corresponds to the seconds elapsed between each transaction and the first transaction in the dataset. ‘Amount’ is the transaction Amount. Finally, ‘Class’ is the response variable and it takes the value 1 in case of fraud and 0 otherwise. Given that the response variable has two possible values (0 for genuine and 1 for fraud), we will create a binary classification Neural Network using TensorFlow, an open source platform for machine learning, and the keras Python/R package, one of the most used deep learning framework built on top of TensorFlow to make it easier to create neural network models.\n\nSetup • Download the dataset from Moodle as a CSV file called creditcard_sample.csv • Visit Google Colab at https://colab.research.google.com/ (make sure you are signed-in to a Google account). You can learn more about what Google Colab is by watching the video below:\n\nhttps://youtu.be/inN8seMm7UI?si=13bM9anVHQrXc_7m \f• In Google Colab, you can create Python or R notebooks to run analytics. Notebooks can be created from scratch, or uploaded as .ipynb files. Two notebook files are available on Moodle:\n\no Week3_Hands_On_Deep_Dive_Python.ipynb o Week3_Hands_On_Deep_Dive_R.ipynb\n\nThey contain Python and R code respectively for this week’s Hands-On Deep Dive activity. These files are ready for you to download from Moodle (under Week 3 section), then upload to Google Colab. Feel free to select your language of choice (Python or R) for this activity.\n\nAlternatively, if you would like to create a notebook from scratch and populate it with Python or R code, follow the links below: o Create a notebook by visiting https://colab.research.google.com/#create=true or simply click File → New notebook (Python is the default language on Google Colab). o If you would like to use R as the runtime language on your new notbook, click “Runtime”, then “Change runtime type” and select\n\n• Once your notebook is uploaded/created and opened, you need to upload the dataset creditcard_sample.csv. Simply open the Files tab in your notebook and upload the file.\n\n• Run your notebook step by step until all cells are executed successfully. Refer to the Python and R notebooks below for the code to be used. After running the code, answer the following question: \fWhat is the impact of the credit card fraud detection model accuracies on customer experience?\n\nNotes \f PYTHON CODE\n\nLoad the dependencies The first step is to define the functions and classes (dependencies) to be used to build your machine learning model. We will use the read_csv function from the pandas library to load the csv dataset. We will also use two classes from the Keras library to define your machine learning model. Let's first load dependencies as follows: import pandas as pd from tensorflow.keras.models import Sequential from tensorflow.keras.layers import Dense from sklearn.metrics import confusion_matrix from sklearn.metrics import classification_report\n\nLoad the data First, download the dataset from Moodle as a CSV file called creditcard_sample.csv. Once downloaded, upload the csv file on your Colab notebook. The response variable in the dataset has two possible values (0 for genuine and 1 for fraud). As such, it is a binary classification problem. All of the other input variables that describe each transaction are numerical. This makes it easy to use directly with neural networks that expect numerical input and output values. Let's load the dataset from the csv file provided as follows: dataset = pd.read_csv('creditcard_sample.csv', skiprows=[1])\n\nPrepare the data There are 30 input variables and one output variable (the last column). We will be training a model to map rows of input variables (X) to an output variable (y). Now, let's split the data into input (X) and output (y) variables as follows: X = dataset[dataset.columns[:-1]] y = dataset.iloc[:,-1:].astype(int)\n\nDefine a model Models in Keras are defined as a sequence of layers. We create a Sequential model and add layers one at a time until we are satisfied with our network architecture. First, we need to ensure that the input layer has the correct number of input features. This can be specified when creating the first layer with the input_shape argument and setting the argument input_shape to (30,) which captures the 30 input variables as a vector. In this example, we use a fully-connected network structure with three layers. Fully connected layers are defined using the Dense class. You can specify the number of neurons or nodes in the layer as the first argument and the activation function using the activation argument. Also, we use the rectified linear unit activation function referred to as ReLU on the first two layers and the Sigmoid function in the output layer. You can combine all layers by adding each one as follows: • The model expects rows of data with 30 variables (the input_shape=(30,) argument). • The first hidden layer includes 12 nodes and uses the relu activation function. • The second hidden layer includes 6 nodes and uses the relu activation function. \f • The output layer includes one node and uses the sigmoid activation function.\n\nLet's define the keras model in python: model = Sequential() model.add(Dense(12, input_shape=(30,), activation='relu')) model.add(Dense(6, activation='relu')) model.add(Dense(1, activation='sigmoid'))\n\nCompile the model Now that the model is defined, let's compile it to obtain the best set of weights to map inputs to outputs in your dataset. In this step, we must specify the loss function to use to evaluate a set of weights, the optimizer used to search through different weights for the network, and any optional metrics you want to collect and report during training. In this case, we use cross entropy as the loss argument. This loss is for a binary classification problem and is defined in Keras as “binary_crossentropy“. We define the optimizer as the efficient stochastic gradient descent algorithm “adam“. This is a popular version of gradient descent because it automatically tunes itself and generates good results in a wide range of problems. Finally, because this is a classification problem, we assess the classification accuracy defined via the metrics argument. Let's compile the model as follows: model.compile(loss='binary_crossentropy', optimizer='adam', metrics=['accuracy'])\n\nTrain the model Now that the model is compiled, let's start training it with data. The training process will run for a fixed number of epochs (iterations) through the dataset that we must specify using the epochs argument. Also, we set the number of dataset rows that are considered before the model weights are updated within each epoch, called the batch size, and set it using the batch_size argument. Let's train the model for a small number of epochs (5) and use a relatively small batch size of 200. model.fit(X, y, epochs=5, batch_size=200)\n\nEvaluate the model Now that we have successfully trained the neural network on the entire dataset, we evaluate the performance of the network on the same dataset. The evaluate() function will return two values. The first value would be the loss of the model, and the second value would be the accuracy of the model. We are only interested in reporting the accuracy so ignore the loss value. _, accuracy = model.evaluate(X, y) print('Accuracy: %.2f' % (accuracy*100))\n\nPredict Finally, we can make predictions by calling the predict() function on the model. We are using a sigmoid activation function on the output layer, therefore the predictions will be probabilities in the range between 0 and 1. You can easily convert them into a binary prediction (0 if prediction is less than 0.5, 1 otherwise). # make class predictions with the model predictions = (model.predict(X) > 0.5).astype(int) print(confusion_matrix(y, predictions)) print(classification_report(y, predictions)) \f R CODE\n\nLoad the dependencies The first step is to define the functions and classes (dependencies) to be used to build your machine learning model. We will use keras library to define your machine learning model. We will also use the dplyr library for data manipulation. Let's first install the keras R package as follows: (note that it may take a couple of minutes to install) # Install the keras package install.packages(\"keras\")\n\nLet's load dependencies as follows: # Load the keras library for fitting neural network models library(keras) # Load the dplyr library for data manipulation library(dplyr)\n\nLoad the data First, download the dataset from Moodle as a CSV file called creditcard_sample.csv. Once downloaded, upload the csv file on your Colab notebook. The response variable in the dataset has two possible values (0 for genuine and 1 for fraud). As such, it is a binary classification problem. All of the other input variables that describe each transaction are numerical. This makes it easy to use directly with neural networks that expect numerical input and output values. Let's load the dataset from the csv file provided as follows: # Load the dataset creditcard <- read.csv(\"creditcard_sample.csv\", header = TRUE, sep = \",\")\n\nPrepare the data There are 30 input variables and one output variable (the last column called Class). We will be training a model to map rows of input variables (X) to an output variable (y). # Prepare the data for fitting a neural network X <- creditcard %>% select(-Class) %>% scale() y <- creditcard$Class\n\nDefine a model Models in Keras are defined as a sequence of layers. We create a Sequential model and add layers one at a time until we are satisfied with our network architecture. First, we need to ensure that the input layer has the correct number of input features. This can be specified when creating the first layer with the input_shape argument and setting the argument input_shape to (30,) which captures the 30 input variables as a vector. In this example, we use a fully-connected network structure with three layers. Fully connected layers are defined using the Dense class. You can specify the number of neurons or nodes in the layer as the first argument and the activation function using the activation argument. Also, we use \fthe rectified linear unit activation function referred to as ReLU on the first two layers and the Sigmoid function in the output layer. You can combine all layers by adding each one as follows:\n\n• The model expects rows of data with 30 variables, which is the number of columns in X (the input_shape = ncol(X) argument). • The first hidden layer includes 12 nodes and uses the relu activation function. • The second hidden layer includes 6 nodes and uses the relu activation function. • The output layer includes one node and uses the sigmoid activation function.\n\nLet's define the keras model in R: # Initialise a Neural Network model model <- keras_model_sequential() # Define the layers of the Neural Network model model %>% layer_dense(units = 12, activation = 'relu', input_shape = ncol(X)) %>% layer_dense(units = 6, activation = 'relu') %>% layer_dense(units = 1, activation = 'sigmoid')\n\nCompile the model Now that the model is defined, let's compile it to obtain the best set of weights to map inputs to outputs in your dataset. In this step, we must specify the loss function to use to evaluate a set of weights, the optimizer used to search through different weights for the network, and any optional metrics you want to collect and report during training. In this case, we use cross entropy as the loss argument. This loss is for a binary classification problem and is defined in Keras as “binary_crossentropy“. We define the optimizer as the efficient stochastic gradient descent algorithm “adam“. This is a popular version of gradient descent because it automatically tunes itself and generates good results in a wide range of problems. Finally, because this is a classification problem, we assess the classification accuracy defined via the metrics argument. Let's compile the model as follows: # Compile the model model %>% compile( loss = 'binary_crossentropy', optimizer = 'adam', metrics = c('accuracy'))\n\nTrain the model Now that the model is compiled, let's start training it with data. The training process will run for a fixed number of epochs (iterations) through the dataset that we must specify using the epochs argument. Also, we set the number of dataset rows that are considered before the model weights are updated within each epoch, called the batch size, and set it using the batch_size argument. Let's train the model for a small number of epochs (5) and use a relatively small batch size of 200. # Fit the model to the data model %>% fit( X, y, epochs = 5, batch_size = 200) \fPredict Finally, we can make predictions by calling the predict() function on the model. We are using a sigmoid activation function on the output layer, therefore the predictions will be probabilities in the range between 0 and 1. You can easily convert them into a binary prediction (0 if prediction is less than 0.5, 1 otherwise). We can then display a confusion matrix, that is a table displaying the number of true positives/negatives and false positive/negatives. In other words, the confusion matrix shows how often the model correctly or incorrectly predicted the outcome. # Make predictions then display the confusion matrix preds = model %>% predict(X) table(as.factor(preds > 0.5), as.factor(y == 1))\n\nWe can also calculate the accuracy, precision, sensitivity, fscore and specificity of the predictions cm = table(as.factor(preds > 0.5), as.factor(y == 1)) accuracy <- sum(cm[1], cm[4]) / sum(cm[1:4]) precision <- cm[4] / sum(cm[4], cm[2]) sensitivity <- cm[4] / sum(cm[4], cm[3]) fscore <- (2 * (sensitivity * precision))/(sensitivity + precision) specificity <- cm[1] / sum(cm[1], cm[2]) message(\"Accuracy: \", accuracy) message(\"Precision: \", precision) message(\"Sensitivity: \", sensitivity) message(\"Fscore: \", fscore) message(\"Specificity: \", specificity)"}